#include <stdio.h>

int main() 
{

    int foo = 1;
    int bar = 1;

    if (foo < bar) {
        printf("foo's is short lol!\n");
    }

    else if (foo == bar) {
        printf("your swords are equal brothers!\n");
    }

    else {
        printf("foo has bigger one, respect!\n");
    }

    return 0;

}